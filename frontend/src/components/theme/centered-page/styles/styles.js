// ./src/components/theme/centered-page/styles/styles.js

// External package dependencies.
import { makeStyles }from '@material-ui/core/styles'

export default makeStyles(theme => ({
  paper: {
    backgroundColor: '#FFFFFF',
  },
}));
